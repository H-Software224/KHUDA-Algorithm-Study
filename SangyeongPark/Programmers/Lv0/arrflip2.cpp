#include <string>
#include <vector>

using namespace std;

/*
string solution(string my_string, vector<vector<int>> queries) {
    for(int i =0; i < queries.size(); i++) {
        string new_str = my_string.substr(queries[i][0], queries[i][1]);
        for(int j = 0; j < new_str.size(); j++) {
            
        }
        string my_string = my_string.substr(0, queries[i][0] -1) + new_str + my_string.substr(queries[i][1] +1, my_string.size());
    }
    return my_string;
}
*/